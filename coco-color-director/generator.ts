import type{ColorDirectorInput,ColorSystem,NormalizedColor,PaletteRoleAssignment,Hex}from"./types.ts";import{PRESETS}from"./presets.ts";import{palettePolicy,saturationPolicy,temperaturePolicy,contrastPolicy}from"./policy.ts";import{colorDistance,darken,desaturate,ensureContrast,lighten,mixColors,rotateHue,saturate,bestTextColor,contrastRatio}from"./colorMath.ts";import{scoreColorSystem}from"./scoring.ts";
export function buildColorCandidate(input:ColorDirectorInput,colors:NormalizedColor[],variant:number):ColorSystem{const policy=palettePolicy(input),preset=PRESETS[policy],nonSkin=colors.filter(c=>c.source!=="skin"),base=nonSkin.length?nonSkin:colors;let dominant=base[variant%base.length]?.hex??preset.dominant;if(policy==="tropical-emerald")dominant=darken(saturate(rotateHue(dominant,130-(variant%3)*8),.12),.12);else if(policy==="industrial-monochrome")dominant=darken(desaturate(dominant,.8),.18);else if(policy==="champagne-black")dominant=darken(desaturate(dominant,.65),.28);else dominant=variant%2?desaturate(dominant,.08):darken(dominant,.08);let support=base.find(c=>colorDistance(c.hex,dominant)>.18)?.hex??preset.support;if(colorDistance(support,dominant)<.18)support=lighten(desaturate(support,.25),.35);let accent=base.filter(c=>colorDistance(c.hex,dominant)>.24&&colorDistance(c.hex,support)>.18)[variant%Math.max(1,base.length)]?.hex??preset.accent;accent=policy==="industrial-monochrome"?desaturate(accent,.65):saturate(accent,.12);const neutral=mixColors(desaturate(support,.45),preset.neutral,.55),dark=darken(desaturate(dominant,.15),.24),light=lighten(desaturate(support,.25),.28);const bg=dominant,headline=ensureContrast(light,bg,4.5),metadata=ensureContrast(mixColors(headline,neutral,.35),bg,4.0),venue=ensureContrast(mixColors(metadata,neutral,.2),bg,3.6),accentText=ensureContrast(accent,bg,3.2),badgeText=bestTextColor(accent),utility=ensureContrast(support,bg,3);const roles:PaletteRoleAssignment[]=[r("background",bg,undefined,"critical","Primary field"),r("backgroundSecondary",dark,undefined,"medium","Depth field"),r("headline",headline,bg,"critical","Strongest text contrast"),r("accent",accentText,bg,"high","Controlled personality color"),r("metadata",metadata,bg,"high","Readable secondary information"),r("dateTime",metadata,bg,"high","Logistics color"),r("venue",venue,bg,"medium","Quiet readable venue"),r("badgeBackground",accent,bg,"medium","Secondary offer anchor"),r("badgeText",badgeText,accent,"high","Readable badge text"),r("presenter",venue,bg,"low","Quiet presenter"),r("footer",venue,bg,"low","Quiet footer"),r("stroke",mixColors(bg,"#000000",.35),undefined,"low","Edge support"),r("glow",mixColors(headline,accent,.2),bg,"low","Palette-derived glow"),r("neutral",neutral,bg,"medium","Neutral support"),r("utility",utility,bg,"medium","Utility support")];const castStrength=input.userPreferences?.allowBackgroundColorCast===false?0:Math.min(input.creativeDirection?.effects?.colorCast??input.scene?.creativeDecisions?.effectPolicy?.colorCast??.14,.38);const partial:Omit<ColorSystem,"score">={id:`color-${policy}-${variant+1}`,name:`${policy} ${variant+1}`,policy,saturationPolicy:saturationPolicy(input),temperaturePolicy:temperaturePolicy(input),contrastPolicy:contrastPolicy(input),skinTonePolicy:(input.imageSignals.skinTones?.length?"protect-strongly":"not-applicable"),colors:{dominant,support,accent,neutral,dark,light},roles,strongColors:[dominant,accent,support].slice(0,input.userPreferences?.maxStrongColors??input.creativeDirection?.palette?.maxStrongColors??3),generatedFromImage:base.length>0,colorCast:{enabled:castStrength>.01,color:mixColors(dominant,accent,.18),strength:castStrength,blendMode:policy==="electric-night"?"screen":policy==="industrial-monochrome"?"color":policy==="champagne-black"?"multiply":"soft-light"},reasoning:[`Policy ${policy}`,`Generated from ${base.length} image colors`],warnings:[]};return{...partial,score:scoreColorSystem(input,partial,colors)}}
function r(role:any,color:Hex,against:Hex|undefined,importance:any,reason:string):PaletteRoleAssignment{return{role,color,contrastAgainst:against,contrastRatio:against?contrastRatio(color,against):undefined,importance,reason}}
