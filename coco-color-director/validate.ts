import type{ColorDirectorInput,ColorSystem}from"./types.ts";export function validateInput(i:ColorDirectorInput){const e:string[]=[];if(!i.imageSignals)e.push("imageSignals required");return e}export function validateSystem(s:ColorSystem){const e:string[]=[];if(!s.id)e.push("id required");if(!s.roles.length)e.push("roles required");for(const[k,v]of Object.entries(s.colors))if(!/^#[0-9A-F]{6}$/i.test(v))e.push(`${k} invalid`);if(!s.roles.some(x=>x.role==="background"))e.push("background role required");if(!s.roles.some(x=>x.role==="headline"))e.push("headline role required");return e}
