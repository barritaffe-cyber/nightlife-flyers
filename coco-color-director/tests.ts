import{directCocoColor}from"./director.ts";import{buildColorRenderModel}from"./renderModel.ts";import{validateRenderedColors}from"./compliance.ts";import{MOJITO_COLOR_FIXTURE}from"./fixtures.ts";const assert=(v:unknown,m:string)=>{if(!v)throw new Error(m)};export function runColorDirectorTests(){const r=directCocoColor(MOJITO_COLOR_FIXTURE);assert(r.winner.policy==="tropical-emerald","policy");assert(r.winner.strongColors.length<=3,"strong colors");const m=buildColorRenderModel(r.winner,3),s={roleColors:Object.fromEntries(m.roles.map(x=>[x.role,x.color])),strongColors:r.winner.strongColors,appliedColorCast:m.colorCast.enabled?{color:m.colorCast.color,strength:m.colorCast.strength,blendMode:m.colorCast.blendMode}:undefined,localContrastRatios:Object.fromEntries(m.roles.filter(x=>x.contrastRatio!==undefined).map(x=>[x.role,x.contrastRatio])),previewExportMatch:true};const c=validateRenderedColors(m,s);assert(c.pass,c.blockers.join(","));return r.winner}
