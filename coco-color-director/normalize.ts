import type{ColorDirectorInput,NormalizedColor}from"./types.ts";import{hexToHsl,hexToRgb,normalizeHex,relativeLuminance,warmthScore}from"./colorMath.ts";
export function normalizeColors(input:ColorDirectorInput):NormalizedColor[]{const out:NormalizedColor[]=[];const add=(hex:string,weight:number,source:NormalizedColor["source"])=>{const h=normalizeHex(hex);if(out.some(x=>x.hex===h&&x.source===source))return;const hsl=hexToHsl(h);out.push({hex:h,rgb:hexToRgb(h),hsl,luminance:relativeLuminance(h),saturation:hsl.s,warmth:warmthScore(h),weight,source})};for(const x of input.imageSignals.sampledColors??[])add(x.hex,x.weight??1,x.source??"unknown");for(const x of input.imageSignals.dominantColors??[])add(x,.85,"background");for(const x of input.imageSignals.skinTones??[])add(x,.9,"skin");for(const x of input.availableBrandColors??[])add(x,.7,"brand");if(!out.length){add("#111111",1,"background");add("#F4EBDD",.8,"generated");add("#C8A96B",.7,"generated")}return out.sort((a,b)=>b.weight-a.weight)}
