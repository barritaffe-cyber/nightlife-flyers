import type {CreativeDirection} from './types.ts';
export type RenderedDesignSnapshot={headlinePower:number;accentPower:number;bodyPower:number;datePower:number;venuePower:number;badgePower:number;presenterPower:number;fontFamilies:string[];strongColors:string[];effects:{glow:number;texture:number;particles:number;blur:number};usedSignatureMoves:string[];previewExportMatch:boolean;protectionViolations:Array<{target:string;overlapRatio:number;allowed:number}>};
export function validateRenderedDesignAgainstDirection(d:CreativeDirection,s:RenderedDesignSnapshot){const blockers:string[]=[],warnings:string[]=[];if(s.accentPower>s.headlinePower*d.hierarchy.accentMaxRatio)blockers.push('accent too strong');if(s.bodyPower>s.headlinePower*d.hierarchy.bodyMaxRatio)blockers.push('body too strong');if(s.datePower>s.headlinePower*d.hierarchy.dateMaxRatio)warnings.push('date too strong');if(s.venuePower>s.headlinePower*d.hierarchy.venueMaxRatio)warnings.push('venue too strong');if(s.badgePower>s.headlinePower*d.hierarchy.badgeMaxRatio)warnings.push('badge too strong');if(new Set(s.fontFamilies).size>d.typography.maxFontFamilies)blockers.push('too many fonts');if(new Set(s.strongColors).size>d.palette.maxStrongColors)warnings.push('too many strong colors');if(s.effects.glow>d.effects.maxGlow)blockers.push('glow exceeds contract');if(s.effects.texture>d.effects.maxTexture)warnings.push('texture exceeds contract');if(s.effects.particles>d.effects.maxParticles)warnings.push('particles exceed contract');if(s.effects.blur>d.effects.maxBlur)warnings.push('blur exceeds contract');if(s.usedSignatureMoves.length>1)blockers.push('more than one signature move');if(!s.previewExportMatch)blockers.push('preview export mismatch');if(s.protectionViolations.some(v=>v.overlapRatio>v.allowed))blockers.push('protection violation');return{pass:blockers.length===0,blockers,warnings,score:Math.max(0,100-blockers.length*22-warnings.length*7)}}
