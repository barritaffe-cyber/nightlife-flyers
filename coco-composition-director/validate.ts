import type { CompositionDirectorInput, CompositionCandidate } from "./types.ts";
import { contains, safeRect } from "./geometry.ts";
export function validateCompositionInput(i:CompositionDirectorInput){const e:string[]=[];if(!i.scene)e.push("scene is required");if(!i.creativeDirection)e.push("creativeDirection is required");if(!i.format)e.push("format is required");if(!i.text)e.push("text is required");if(!i.creativeDirection?.composition)e.push("creativeDirection.composition is required");return e}
export function validateCompositionCandidate(c:CompositionCandidate,i:CompositionDirectorInput){const e:string[]=[];if(!c.blocks.some(b=>b.role==="identity"&&!b.hidden))e.push("identity block is required");if(!c.owns.includes("headline"))e.push("composition must own headline");if(c.score.hardViolations>0)e.push("winning composition contains hard violations");if(!contains(safeRect(i.format),c.textColumn))e.push("text column is outside safe bounds");if(i.creativeDirection.composition.oneColumn&&!c.textColumn)e.push("one-column direction requires textColumn");return e}
